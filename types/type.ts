export type Post = {
    index:number,
    title: string,
    description: string,
    createdAt: string,
    updatedAt: string,
    thumbnail: string,
    categories: string[],
    keywords: string[],
};